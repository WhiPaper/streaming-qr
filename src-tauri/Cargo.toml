[package]
name = "streaming-qr"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tauri = { version = "2.0.0-rc.6", features = ["wry"] }

[build-dependencies]
tauri-build = { version = "2.0.0-rc.6" }
